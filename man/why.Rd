% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/why.R
\name{why}
\alias{why}
\title{Why: Multi-trace reasoning with verification}
\usage{
why(
  prompt,
  reasoner,
  verifier = reasoner,
  n_traces = 5L,
  max_steps = 12L,
  temp_sample = 0.7,
  temp_verify = 0
)
}
\arguments{
\item{prompt}{Character string. The question or problem to reason about.}

\item{reasoner}{A model object. The stronger model used for reasoning
and verification (if no separate verifier is provided).}

\item{verifier}{A model object. Optional cheaper/faster model used for
sampling traces. Defaults to the same model as \code{reasoner}.}

\item{n_traces}{Integer. Number of reasoning traces to generate.
Recommended range is 4-8. Default is \code{5L}.}

\item{max_steps}{Integer. Maximum number of reasoning steps per trace.
Default is \code{12L}.}

\item{temp_sample}{Numeric. Temperature parameter for sampling traces.
Higher values produce more diverse outputs. Default is \code{0.7}.}

\item{temp_verify}{Numeric. Temperature parameter for verification.
Lower values produce more deterministic verification. Default is \code{0.0}.}
}
\value{
The verified response or best solution trace (return type depends
on implementation).
}
\description{
Implements a reasoning pipeline that generates multiple solution traces
using a reasoner model, then verifies and selects the best response.
This approach improves reliability by sampling diverse reasoning paths
and using verification to identify the most accurate solution.
}
